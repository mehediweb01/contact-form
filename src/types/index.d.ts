export interface IPerson {
  id: number;
  name: string;
  email: string;
}
export interface ContactForm {
  firstName?: string;
  lastName?: string;
  name: string;
  email: string;
  phone: string;
  message: string;
}
