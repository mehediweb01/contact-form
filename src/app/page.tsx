import { Header } from "@/components";
import React from "react";

const page = () => {
  return (
    <div className="bg-background h-screen ">
      <Header />
    </div>
  );
};

export default page;
